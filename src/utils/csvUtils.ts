import fs from 'fs';
import { ReadLine } from 'readline';
interface CSVRecords{
    [key:string]:string;
}

export async function csvUtils(fileName:string):Promise<CSVRecords[]> {
  const result: CSVRecords[] = [];
    return result;
}